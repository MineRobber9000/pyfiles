"""Your challenge is to write a program that will pull whatever value is in d["key"] below.
For instance, if d["key"] is "foo", as it is in this example module, then the program should print "foo"."""

d = {"key":"foo"}

# prevent import
raise Exception(
"""Not gonna let you get away with it that easily!

If I just let you import the module, it'd be too easy, see? So instead
I want you to figure out another way.""")
